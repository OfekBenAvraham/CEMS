package enums;

/**
 * Represents the type of operations that can be performed on a repository in the system.
 * This enumeration contains three possible types of operations: Personal, Search, and ToActive.
 */
public enum RepositoryOperations {
	Personal, Search, ToActive
}
